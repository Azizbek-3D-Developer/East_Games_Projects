version: "3.9"
services:
  app:
    build: .
    ports:
      - "9000:9000"
    environment:
      - APP_PORT=9000
      - UPLOAD_DIR=uploads
      - TOP_K_WORDS=50
      - APP_VERSION=3.0.0
    volumes:
      - .:/app
    command: sh -c "uvicorn app:app --host 0.0.0.0 --port ${APP_PORT}"
